// Brand color values — source of truth for Ant Design's ConfigProvider tokens.
// These must stay in sync with the CSS variables defined in index.css.
export const theme = {
  primary: '#184c45', // --primary-green
  secondary: '#4f7a67', // --secondary-green
} as const
